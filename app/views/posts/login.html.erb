<!DOCTYPE html>
    <html>
    <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, width=device-width"/>
    <title>Login Demo - Kakao JavaScript SDK</title>
    <script src="//developers.kakao.com/sdk/js/kakao.min.js"></script>

    </head>
    <body>
    <a id="kakao-login-btn"></a>
     <!--href="javascript:loginWithKakao()"-->
    <!--<a href="http://developers.kakao.com/logout"></a>-->
    <script type='text/javascript'>
    
    
      var authObj = "";
      var tt = "";
      var tok = "";
      //<![CDATA[
        // 사용할 앱의 JavaScript 키를 설정해 주세요.
        Kakao.init('b5020875994670303b9485f77edd4d60');
          
        // 카카오 로그인 버튼을 생성합니다.
        Kakao.Auth.createLoginButton({
          container: '#kakao-login-btn',
          success: function(authObj) {
            Kakao.API.request({
              url: '/v1/user/me',
              success: function(res) {
              tt=(JSON.stringify(res));
              document.write(tt);
              }
            });
            // alert(JSON.stringify(authObj));
          
            tok=JSON.stringify(authObj);
            // tok=JSON.parse(JSON.stringify(authObj));
            document.write(tok);
          },
          fail: function(err) {
             alert(JSON.stringify(err));
          }


        });

    
      //]]>
    </script>

    
    <a href="https://sukhwan180724-2-rectuspin.c9users.io/posts/signup">회원가입</a>
    </body>
    </html>
